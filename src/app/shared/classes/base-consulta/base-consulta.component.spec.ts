import { ComponentFixture, TestBed } from '@angular/core/testing';

import { BaseConsultaComponent } from './base-consulta.component';

describe('BaseConsultaComponent', () => {
  let component: BaseConsultaComponent<any>; // Add the missing type argument 'any'
  let fixture: ComponentFixture<BaseConsultaComponent<any>>; // Add the missing type argument 'any'


});
