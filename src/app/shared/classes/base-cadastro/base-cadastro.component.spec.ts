import { ComponentFixture, TestBed } from '@angular/core/testing';

import { BaseCadastroComponent } from './base-cadastro.component';

describe('BaseConsultaComponent', () => {
  let component: BaseCadastroComponent<any>; // Add the missing type argument 'any'
  let fixture: ComponentFixture<BaseCadastroComponent<any>>; // Add the missing type argument 'any'


});
